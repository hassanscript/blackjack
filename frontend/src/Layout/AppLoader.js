import loadIcon from "../Assets/loadIcon.png";
import { Loader } from "../Components";

const AppLoader = () => {
  return (
    <div id="main-loader">
      <Loader />
    </div>
  );
};

export default AppLoader;
