import loadIcon from "../Assets/loadIcon.png";

const Loader = () => {
  return (
    <div id="loader">
      <div>
        <img src={loadIcon} />
      </div>
    </div>
  );
};

export default Loader;
